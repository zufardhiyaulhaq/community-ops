apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: wildcard-knative-zufardhiyaulhaq-tech-authz
  namespace: istio-system
spec:
  selector:
    matchLabels:
      app: istio-knative-public-gateway
  action: ALLOW
  rules:
  - to:
    - operation:
        hosts: 
        - "*.knative.zufardhiyaulhaq.tech"
        - "*.knative.zufardhiyaulhaq.tech:443"
