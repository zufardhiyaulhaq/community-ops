apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: test-vcluster-01-kubernetes-zufardhiyaulhaq-com-authz
  namespace: istio-system
spec:
  selector:
    matchLabels:
      app: istio-public-gateway
  action: ALLOW
  rules:
  - to:
    - operation:
        ports:
        - "20443"
    when:
    - key: connection.sni
      values:
      - test-vcluster-01.kubernetes.zufardhiyaulhaq.com
